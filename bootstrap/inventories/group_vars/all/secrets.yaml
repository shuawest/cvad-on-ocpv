---

# secrets_file: "{{ playbook_dir }}/../../../environment-secrets.yaml"

#--extra-vars "ansible_python_interpreter=/usr/bin/python3"
# ansible-playbook -i inventories/hosts-local.ini -e  playbooks/openshift/facts.yaml

# ansible-playbook --extra-vars "secrets_file=/Users/jowest/dev/repos/cvad-on-ocpv/env-hzz27-aws.yaml" -i inventories/hosts-local.ini -e  playbooks/openshift/facts.yaml

env_auth_secrets: "{{ playbook_dir }}/../../../env-auth.yaml"
